# Create the real secret with: kubectl create secret generic triage-agent-keys -n support-agents --from-literal=ANTHROPIC_API_KEY=sk-ant-... [--dry-run=client -o yaml | kubectl apply -f -]
# Do NOT commit secret.yaml with real keys. This example shows the shape only.
apiVersion: v1
kind: Secret
metadata:
  name: triage-agent-keys
  namespace: support-agents
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "<your-anthropic-api-key>"
