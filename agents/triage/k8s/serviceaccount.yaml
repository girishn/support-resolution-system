# Service account for the triage agent. Used with EKS Pod Identity to access DynamoDB.
# Create this before deploying the triage agent when using DYNAMODB_TABLE.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: triage-agent
  namespace: support-agents
