apiVersion: apps/v1
kind: Deployment
metadata:
  name: technical-agent
  namespace: support-agents
  labels:
    app: technical-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: technical-agent
  template:
    metadata:
      labels:
        app: technical-agent
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9092"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: technical
          image: "233422680010.dkr.ecr.us-east-1.amazonaws.com/technical-agent:latest"
          imagePullPolicy: IfNotPresent
          ports:
            - name: metrics
              containerPort: 9092
              protocol: TCP
          envFrom:
            - configMapRef:
                name: technical-agent-config
            - secretRef:
                name: technical-agent-keys
                optional: true
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      restartPolicy: Always
