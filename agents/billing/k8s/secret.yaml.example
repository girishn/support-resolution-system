# Create: kubectl create secret generic billing-agent-keys -n support-agents --from-literal=ANTHROPIC_API_KEY=sk-ant-... [--dry-run=client -o yaml | kubectl apply -f -]
apiVersion: v1
kind: Secret
metadata:
  name: billing-agent-keys
  namespace: support-agents
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "<your-anthropic-api-key>"
